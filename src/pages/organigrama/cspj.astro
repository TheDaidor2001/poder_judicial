---
import MainLayout from "../../layouts/MainLayout.astro";
import { miembrosConsejo, ordenCargos } from "../../config/constants/consejo";

// Función para ordenar miembros según el orden de cargos definido
function ordenarMiembros(
    miembros: {
        id: string;
        cargo: string;
        nombre: string;
        foto: string;
        descripcion: string;
        funciones: string[];
        trayectoria: string;
    }[],
    orden: string | any[],
) {
    return [...miembros].sort((a, b) => {
        const indexA = orden.indexOf(a.cargo);
        const indexB = orden.indexOf(b.cargo);

        // Si el cargo no está en el orden, lo pone al final
        const posA = indexA === -1 ? orden.length : indexA;
        const posB = indexB === -1 ? orden.length : indexB;

        return posA - posB;
    });
}

const miembrosOrdenados = miembrosConsejo;
---

<MainLayout
    title="Poder Judicial de Guinea Ecuatorial | Consejo Superior del Poder Judicial"
>
    <!-- Hero Section -->
    <section class="bg-primary text-white py-20">
        <div class="max-w-7xl mx-auto px-5 text-center">
            <h1 class="text-4xl md:text-5xl font-bold mb-6">
                Consejo Superior del Poder Judicial
            </h1>
            <p class="text-xl md:text-2xl max-w-3xl mx-auto">
                Órgano de gobierno del Poder Judicial de Guinea Ecuatorial
            </p>
        </div>
    </section>

    <!-- Members Grid -->
    <section class="max-w-7xl mx-auto px-3 md:px-5 py-16">
        <!-- Fila 1: Presidente (1 card) -->
        <div class="grid grid-cols-1 gap-8 mb-8 max-w-sm md:max-w-md mx-auto">
            {
                miembrosOrdenados.slice(0, 1).map((miembro) => (
                    <article class="bg-white rounded-xl shadow-lg overflow-hidden transition-transform hover:shadow-xl hover:-translate-y-1">
                        <div class="h-96 md:h-[28rem] bg-blue-100 flex items-center justify-center overflow-hidden">
                            <img
                                src={miembro.foto}
                                alt={miembro.nombre}
                                class="h-full w-full object-cover object-top"
                            />
                        </div>
                        <div class="p-4 md:p-6">
                            <div>
                                <span class="text-sm font-semibold text-secondary">
                                    {miembro.cargo
                                        .replace(
                                            "del Consejo Superior del Poder Judicial",
                                            "",
                                        )
                                        .trim()}
                                </span>
                                <h2 class="text-xl font-bold text-gray-800 mt-1">
                                    {miembro.nombre}
                                </h2>
                            </div>
                        </div>
                    </article>
                ))
            }
        </div>

        <!-- Fila 2: Vicepresidente (1 card) -->
        <div class="grid grid-cols-1 gap-8 mb-8 max-w-sm md:max-w-md mx-auto">
            {
                miembrosOrdenados.slice(1, 2).map((miembro) => (
                    <article class="bg-white rounded-xl shadow-lg overflow-hidden transition-transform hover:shadow-xl hover:-translate-y-1">
                        <div class="h-96 md:h-[28rem] bg-blue-100 flex items-center justify-center overflow-hidden">
                            <img
                                src={miembro.foto}
                                alt={miembro.nombre}
                                class="h-full w-full object-cover object-top"
                            />
                        </div>
                        <div class="p-4 md:p-6">
                            <div>
                                <span class="text-sm font-semibold text-secondary">
                                    {miembro.cargo
                                        .replace(
                                            "del Consejo Superior del Poder Judicial",
                                            "",
                                        )
                                        .trim()}
                                </span>
                                <h2 class="text-xl font-bold text-gray-800 mt-1">
                                    {miembro.nombre}
                                </h2>
                            </div>
                        </div>
                    </article>
                ))
            }
        </div>

        <!-- Fila 3: Vocales y Secretarios (4 cards) -->
        <div
            class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 md:gap-8 mb-8"
        >
            {
                miembrosOrdenados.slice(2, 6).map((miembro) => (
                    <article class="bg-white rounded-xl shadow-lg overflow-hidden transition-transform hover:shadow-xl hover:-translate-y-1 mx-auto w-full max-w-sm md:max-w-none">
                        <div class="h-80 md:h-72 lg:h-64 bg-blue-100 flex items-center justify-center overflow-hidden">
                            <img
                                src={miembro.foto}
                                alt={miembro.nombre}
                                class="h-full w-full object-cover object-top"
                            />
                        </div>
                        <div class="p-4 md:p-6">
                            <div>
                                <span class="text-sm font-semibold text-secondary">
                                    {miembro.cargo
                                        .replace(
                                            "del Consejo Superior del Poder Judicial",
                                            "",
                                        )
                                        .trim()}
                                </span>
                                <h2 class="text-xl font-bold text-gray-800 mt-1">
                                    {miembro.nombre}
                                </h2>
                            </div>
                        </div>
                    </article>
                ))
            }
        </div>

        <!-- Fila 4: Directora de Archivos y Director de Económicos (2 cards) -->
        <div
            class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8 mb-8 max-w-4xl mx-auto"
        >
            {
                miembrosOrdenados.slice(6, 8).map((miembro) => (
                    <article class="bg-white rounded-xl shadow-lg overflow-hidden transition-transform hover:shadow-xl hover:-translate-y-1 mx-auto w-full max-w-sm md:max-w-none">
                        <div class="h-80 md:h-72 bg-blue-100 flex items-center justify-center overflow-hidden">
                            <img
                                src={miembro.foto}
                                alt={miembro.nombre}
                                class="h-full w-full object-cover object-top"
                            />
                        </div>
                        <div class="p-4 md:p-6">
                            <div>
                                <span class="text-sm font-semibold text-secondary">
                                    {miembro.cargo
                                        .replace(
                                            "del Consejo Superior del Poder Judicial",
                                            "",
                                        )
                                        .trim()}
                                </span>
                                <h2 class="text-xl font-bold text-gray-800 mt-1">
                                    {miembro.nombre}
                                </h2>
                            </div>
                        </div>
                    </article>
                ))
            }
        </div>

        <!-- Fila 5: Directores (2 cards) -->
        <div
            class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8 max-w-4xl mx-auto"
        >
            {
                miembrosOrdenados.slice(8).map((miembro) => (
                    <article class="bg-white rounded-xl shadow-lg overflow-hidden transition-transform hover:shadow-xl hover:-translate-y-1 mx-auto w-full max-w-sm md:max-w-none">
                        <div class="h-80 md:h-72 bg-blue-100 flex items-center justify-center overflow-hidden">
                            <img
                                src={miembro.foto}
                                alt={miembro.nombre}
                                class="h-full w-full object-cover object-top"
                            />
                        </div>
                        <div class="p-4 md:p-6">
                            <div>
                                <span class="text-sm font-semibold text-secondary">
                                    {miembro.cargo
                                        .replace(
                                            "del Consejo Superior del Poder Judicial",
                                            "",
                                        )
                                        .trim()}
                                </span>
                                <h2 class="text-xl font-bold text-gray-800 mt-1">
                                    {miembro.nombre}
                                </h2>
                            </div>
                        </div>
                    </article>
                ))
            }
        </div>
    </section>

    <style>
        /* Animación suave para las tarjetas */
        article {
            transition: all 0.3s ease;
        }
    </style>
</MainLayout>
